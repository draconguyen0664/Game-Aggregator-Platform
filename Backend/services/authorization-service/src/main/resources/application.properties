spring.application.name=authorization-service
server.port=0
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/authorization_service}
spring.datasource.username=${DB_USERNAME:game_app}
spring.datasource.password=${DB_PASSWORD:change-me}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.flyway.enabled=true
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
management.endpoints.web.exposure.include=health,info,prometheus
platform.core.service-name=${spring.application.name}
platform.core.api-version=v1
security.jwt.secret=${JWT_SECRET:change-this-development-secret-at-least-32-bytes}
