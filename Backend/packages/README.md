# Packages

Shared packages are limited to technical concerns such as observability, test fixtures, and generated contract clients. Domain models and persistence code must remain owned by one service.
