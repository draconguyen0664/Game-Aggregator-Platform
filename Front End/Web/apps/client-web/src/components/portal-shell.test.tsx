import {describe,expect,it} from "vitest";import {clientNavigation} from "./portal-shell";describe("clientNavigation",()=>{it("provides a unique destination for each workflow",()=>{const nav=clientNavigation("games");expect(new Set(nav.map(x=>x.href)).size).toBe(nav.length);expect(nav.find(x=>x.label==="Available games")?.active).toBe(true);expect(nav.find(x=>x.label==="Usage & revenue")?.href).toBe("/#usage")})});
