# Contracts

Store versioned OpenAPI, AsyncAPI, JSON Schema, and protobuf contracts here. A service remains the source of truth for its contract; generated clients may be published under `../packages`.
