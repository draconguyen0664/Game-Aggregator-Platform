# tenant-service
Owns tenants, organizations, memberships, roles, permissions, and role assignments. Database: tenant_service. Every tenant operation verifies JWT subject membership and tenant-scoped permission.
