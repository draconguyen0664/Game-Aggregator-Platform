package com.gameaggregator.webhookservice.api;import jakarta.validation.constraints.*;import java.util.*;import java.util.UUID;public final class WebhookDtos{private WebhookDtos(){}public record Create(@NotNull UUID tenantId,@Pattern(regexp="https://.*")String url,@NotEmpty Set<String> eventSubscriptions,@NotBlank String environment,@Min(100)@Max(30000)int timeoutMs,@Min(0)@Max(10)int maxRetries){}public record Issued(UUID id,String secret){}public record Status(boolean active){} }