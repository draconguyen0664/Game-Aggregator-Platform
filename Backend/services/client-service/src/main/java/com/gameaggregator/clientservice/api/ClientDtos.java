package com.gameaggregator.clientservice.api;import jakarta.validation.constraints.*;import java.time.Instant;import java.util.UUID;public final class ClientDtos{private ClientDtos(){}public record Organization(@NotNull UUID tenantId,@NotBlank String slug,@NotBlank String name){}public record Application(@NotBlank String name){}public record Environment(@NotBlank String name){}public record Allowlist(@Pattern(regexp="IP|DOMAIN") String type,@NotBlank String value){}public record Integration(@Pattern(regexp="PENDING|TESTING|ACTIVE|FAILED|SUSPENDED") String status){}public record Entitlement(@NotNull UUID gameId,@NotBlank String territory,Instant expiresAt){}}
